#1 WE will create a route for a products 
  1. create -> 
  2. update -> 
  3. delete -> it will delete with his variants.
  4. get -> if prod have variants will return.
  5.getAll -> will return with variants.

#2 WEE WILL Have a sub route from products to the variants
  1. create -> check i prod exist then create variant for it
  2. update -> check i prod exist then update variant for it
  3. delete -> check i prod exist then delete variant for it.
  4. get -> we do not need it because it will return automatic with the prod.